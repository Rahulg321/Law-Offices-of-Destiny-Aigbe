import React from "react";
import { Button } from "./ui/button";
import { IoIosAddCircleOutline } from "react-icons/io";

const LoadMoreButton = () => {
  return (
    <Button variant={"outline"} className="px-24 py-6">
      Load More
    </Button>
  );
};

export default LoadMoreButton;
